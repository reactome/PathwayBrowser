import { AuthorshipNameFormatPipe } from './authorship-name-format.pipe';

describe('AuthorshipNameFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AuthorshipNameFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
