import { RemoveProteinNamePipe } from './remove-protein-name.pipe';

describe('RemoveProteinNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveProteinNamePipe();
    expect(pipe).toBeTruthy();
  });
});
