import { CustomSortByPipe } from './custom-sort-by.pipe';

describe('CustomSortByPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomSortByPipe();
    expect(pipe).toBeTruthy();
  });
});
