import { AuthorshipDateFormatPipe } from './authorship-date-format.pipe';

describe('AuthorshipDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AuthorshipDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
